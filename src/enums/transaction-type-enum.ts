export enum TransactionType {
  BY_CARD = 1,
  BY_CASH = 2,
  REFUND = 3,
  CASHBACK = 4,
}

export enum TransactionTypeOutPut {
  BY_CARD = 'С КАРТОЙ',
  BY_CASH = 'НАЛИЧНЫМИ',
  REFUND = 'СДАЧИ',
  CASHBACK = 'КЭШБЭК',
}
